# peiso build output: composed roots, media, compose locks.
/out

# Provium's per-profile build directories, when pointed here.
/.provium-build

# The suite's guest tools, compiled on demand from tests/tools/*.c.
tests/tools/.build/
